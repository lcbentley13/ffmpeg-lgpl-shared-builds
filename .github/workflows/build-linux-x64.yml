name: build-linux-x64

on:
  workflow_call:
    inputs:
      tag:
        description: "FFmpeg Version"
        required: true
        type: string

jobs:
  build:
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Checkout FFmpeg
        uses: actions/checkout@v4
        with:
          repository: FFmpeg/FFmpeg
          ref: n${{ inputs.tag }}
          path: FFmpeg

      - name: Generate README
        env:
          TAG: ${{ inputs.tag || github.event.inputs.tag }}
        run: |
          short_sha="$(git -C FFmpeg rev-parse --short HEAD)"
          echo "SHORT_SHA=${short_sha}" >> "$GITHUB_ENV"
          source_archive="FFmpeg-${TAG}-${short_sha}-source-code.tar.xz"
          sed \
            -e "s|%%Platform%%|linux-x64|g" \
            -e "s|%%FFmpeg-Version%%|${TAG} (${short_sha})|g" \
            -e "s|%%Source-Code-Archive%%|${source_archive}|g" \
            README.md > FFmpeg/README.md
          python3 -c "
          content = open('FFmpeg/README.md').read()
          script = 'sudo apt-get update\nsudo apt-get install -y nasm\n./configure --arch=x86_64 --enable-shared --disable-programs --disable-doc --prefix=output\nmake\nmake install'
          content = content.replace('%%Compilation-Script%%', script)
          open('FFmpeg/README.md', 'w').write(content)
          "

      - name: Install NASM
        run: |
          sudo apt-get update
          sudo apt-get install -y nasm

      - name: Configure
        working-directory: FFmpeg
        run: ./configure --arch=x86_64 --enable-shared --disable-programs --disable-doc --prefix=output

      - name: Build
        working-directory: FFmpeg
        run: make

      - name: Install
        working-directory: FFmpeg
        run: make install

      - name: Prepare artifact
        run: |
          mkdir -p FFmpeg/artifact/lib
          shopt -s nullglob
          libs=(FFmpeg/output/lib/lib*.so)
          if [[ ${#libs[@]} -eq 0 ]]; then
            echo "::error::No .so files found in FFmpeg/output/lib/"
            ls -la FFmpeg/output/lib/ || true
            exit 1
          fi
          cp -L "${libs[@]}" FFmpeg/artifact/lib/
          cp FFmpeg/README.md FFmpeg/artifact/README.md

          artifacts_path="FFmpeg-${{ inputs.tag || github.event.inputs.tag }}-${{ env.SHORT_SHA }}-linux-x64.tar.xz"
          echo "ARTIFACTS_PATH=${artifacts_path}" >> "$GITHUB_ENV"
          tar -cJf "$artifacts_path" FFmpeg/artifact/

      - name: Upload artifacts
        uses: actions/upload-artifact@v7
        with:
          path: ${{ env.ARTIFACTS_PATH }}
